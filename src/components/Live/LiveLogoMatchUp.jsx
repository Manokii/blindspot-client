import React from "react";
import { useSelector } from "react-redux";
import { makeStyles } from "@material-ui/core";

const q = makeStyles((theme) => ({
    root: {},
}));

const LiveLogoMatchUp = () => {
    const c = q();
    return <div></div>;
};

export default LiveLogoMatchUp;
